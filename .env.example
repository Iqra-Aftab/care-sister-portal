# Supabase
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=   # server-only, used by scheduled Edge Functions

# Resend
RESEND_API_KEY=

# Sanity
NEXT_PUBLIC_SANITY_PROJECT_ID=
NEXT_PUBLIC_SANITY_DATASET=production
SANITY_API_TOKEN=            # write token, used only in Studio / migrations

# Companion chat (optional — only if /api/companion is wired to a live LLM)
ANTHROPIC_API_KEY=
